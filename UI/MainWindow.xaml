<Window x:Class="RevitShapeAnalyzer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Shape Analyzer"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Revit Object Analyzer"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,20"/>

        <DataGrid x:Name="dgCounts"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  Background="#2D2D30"
                  Foreground="White"
                  RowBackground="#2D2D30"
                  AlternatingRowBackground="#3E3E42">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Category"
                                    Binding="{Binding Category}"
                                    Width="*"/>

                <DataGridTextColumn Header="Count"
                                    Binding="{Binding Count}"
                                    Width="150"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Window>